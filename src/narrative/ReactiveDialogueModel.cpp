#include "nightwalker/narrative/ReactiveDialogueModel.h"

// The deterministic reaction model is intentionally header-only so pure test targets
// and the native plugin share one implementation without linker-order assumptions.
